**Syft** = inventories what's inside your project (or Docker image) — every package, library, and its exact version. Output = the SBOM (a JSON list).

**Grype** = takes that inventory and checks each item against a database of known CVEs (vulnerabilities), telling you which of your dependencies have publicly known security flaws and how severe they are.

Together: Syft answers _"what's actually in my software?"_ — Grype answers _"is any of that dangerous?"_

For your Inception project specifically: since it's Dockerfiles pulling in NGINX, WordPress, PHP, and MariaDB, the useful version of this test is scanning your **built images** — that's where the real OS packages and library versions live, and where actual CVEs would show



## How 

```bash
# 1. Install
curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh | sh -s -- -b /usr/local/bin
curl -sSfL https://raw.githubusercontent.com/anchore/grype/main/install.sh | sh -s -- -b /usr/local/bin

# 2. Generate SBOM (pick ONE target)
syft <path-to-source>        -o cyclonedx-json=sbom.json   # source code / repo
syft <docker-image-name>     -o cyclonedx-json=sbom.json   # built container image

# 3. Scan the SBOM for known CVEs
grype sbom:sbom.json -o table
```



* For a docker-based project, target the built image:
```bash
docker build -t my-image ./path/to/Dockerfile-dir
syft my-image -o cyclonedx-json=sbom.json
grype sbom:sbom.json -o table
```


> Reference [[JOSA/submit/week7/content/Journal Entry, SBOM & Vulnerability Scan (MariaDB image)|Journal Entry, SBOM & Vulnerability Scan (MariaDB image)]] for a case study on inception project.